"""Swichy ML package — trainable shot-quality models (parallel to the rule engine).

This package does NOT replace ``analysis.BiomechanicsEngine``.
Both systems should run side-by-side until the ML model is proven better.
"""

__version__ = "0.1.0"
