# MediaPipe pose acceleration.
# auto tries the GPU delegate first and falls back to CPU when the installed
# MediaPipe package was not compiled with GPU support.
device: auto # auto | cpu | gpu

# Number of concurrent pose candidates to request from MediaPipe.
# The shooter association layer will pick the ball-related candidate.
num_poses: 8
